services:
  streamer:
    build: .
    container_name: ws-streamer
    restart: unless-stopped
    env_file:
      - ../.env
    user: "1000:1000"
    ipc: host
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "984"
      - "988"
    ports:
      - "8080:8080"
    environment:
      - XDG_RUNTIME_DIR=/run/user/1000
      - DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
      - PIPEWIRE_RUNTIME_DIR=/run/user/1000
      - PORT=8080
      - FRAMERATE=${FRAMERATE:-60}
      - TARGET_WIDTH=${TARGET_WIDTH:-1920}
      - TARGET_HEIGHT=${TARGET_HEIGHT:-1080}
      - VIDEO_BITRATE=${VIDEO_BITRATE:-6000k}
      - AUDIO_BLACKLIST=${AUDIO_BLACKLIST:-discord,Discord,vesktop,webcord,slack,zoom,teams}
      - INCLUDE_MIC=${INCLUDE_MIC:-false}
    volumes:
      - /run/user/1000/bus:/run/user/1000/bus
      - /run/user/1000/pipewire-0:/run/user/1000/pipewire-0
      - /run/user/1000/pulse:/run/user/1000/pulse
      - ./web:/home/streamer/web:ro

  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: ws-cloudflared
    restart: unless-stopped
    env_file:
      - ../.env
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - streamer
